---
import Section from "../Section.astro"
import HTML from "@/icons/html.astro"
import CSS from "@/icons/css.astro"
import JavaScript from "@/icons/javascript.astro"
import TypeScript from "@/icons/type.astro"
import React from "@/icons/react.astro"
import Node from "@/icons/node.astro"
import MySQL from "@/icons/sql.astro"
import Git from "@/icons/git.astro"
import GitHub from "@/icons/GitHub.astro"
import Tailwind from "@/icons/tailwind.astro"
import Next from "@/icons/next.astro"
import { skills } from "@cv"
import X from "@/icons/X.astro"

const SKILLS_ICONS: Record<string, any> = {
  HTML,
  CSS,
  JavaScript,
  TypeScript,
  React,
  Node,
  MySQL,
  Git,
  GitHub,
  Next,
  Tailwind,
}
---

<Section>
  <h3 class="divider">Habilidades</h3>
  <ul>
    <h4>Front end development: </h4>
      {
        skills.map(({ name,keywords }) => {
            const frontend = keywords.find((element) => element == "Frontend")
            if(frontend){
              const iconName = name === "Next.js" ? "Next" : name
              const Icon = SKILLS_ICONS[iconName]

              return (
                <li>
                  {Icon && <Icon />} <span>{name}</span>
                </li>
              )
            }
        })
      }
  </ul>
  <ul>
    <h4>Back end development: </h4>
    {
      skills.map(({ name,keywords }) => {
          const frontend = keywords.find((element) => element == "Backend")
          if(frontend){
            const iconName = name === "Next.js" ? "Next" : name
            const Icon = SKILLS_ICONS[iconName]

            return (
              <li>
                {Icon && <Icon />} <span>{name}</span>
              </li>
            )
          }
      })
    }
  </ul>
  <ul>
    <h4>Miscelaneo: </h4>
    {
      skills.map(({ name,keywords }) => {
          const frontend = keywords.find((element) => element == "Colaboración" || element == "Cloud")
          if(frontend){
            const iconName = name === "Next.js" ? "Next" : name
            const Icon = SKILLS_ICONS[iconName]

            return (
              <li>
                {Icon && <Icon />} <span>{name}</span>
              </li>
            )
          }
      })
    }
  </ul>
  <br>
  <ul>
    <h4>Soft skills: </h4>
    <li> Teamplayer, Bias for action, Deliver results, Español, Ingles</li>
  </ul>
</Section>

<style>
  ul {
    display: inline-flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-bottom: 3px;
  }

  ul h4{
    margin: 0;
  }

  li {
    align-items: center;
    background: #eee;
    border-radius: 6px;
    color: black;
    display: flex;
    font-size: 0.8rem;
    font-weight: 500;
    gap: 4px;
    padding: 0.2rem 0.6rem;
  }
</style>